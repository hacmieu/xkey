# Rebuild 20260813 Stable Build — XKeyIM

**Date:** 2026-08-14 16:40

## Bối cảnh

- Ngày 2026-08-14, các thay đổi tối ưu cùng ngày gây regression → user yêu cầu khôi phục bản ổn định.
- `git reset --hard HEAD` về tag `v1.2.24-20260721` đã xóa toàn bộ working-tree changes.
- Bản **20260813** (Macro hoạt động trong XKeyIM + cursor fix + version linking) **chưa bao giờ được commit** — chỉ tồn tại dưới dạng uncommitted changes.
- Git reflog xác nhận không thể recover: chỉ có clone + checkout operations.
- **Giải pháp:** Re-implement toàn bộ thay đổi 20260813 từ documentation:
  - `memory/20260813_2256-Cursor_Tracking_VSCode_Fix.md`
  - `memory/20260813_2200-Macro_Fix_Completed.md`
  - `memory/20260813_2226-Fix_XKeyIM_Version_Config_Linking.md`
  - `reports/20260813_1648-Macro_System_Analysis_XKeyIM.md`
  - `plans/20260813_1648-Fix_Macro_XKeyIM.md`

## Các thay đổi đã re-implement

### 1. Cursor Tracking False-Positive Fix (VS Code)
**File:** `XKeyIM/XKeyIMController.swift`
- Thêm property `skipNextCursorCheck: Bool = false`
- `handle()`: consume flag `skipThisCursorCheck = skipNextCursorCheck; skipNextCursorCheck = false`, thêm `&& !skipThisCursorCheck` vào điều kiện cursor detection
- `setMarkedText()`: set `skipNextCursorCheck = true` sau khi update composing text
- `commitComposition()`: set `skipNextCursorCheck = true` sau khi commit

### 2. Macro System trong XKeyIM (4 root causes — report 20260813_1648)
**File:** `XKeyIM/XKeyIMController.swift`
- **Root cause #1:** `XKeyIMSettings` thêm 4 property: `macroEnabled`, `macroInEnglishMode`, `autoCapsMacro`, `addSpaceAfterMacro` (all `Bool = false`), đọc từ keys `XKey.macroEnabled`, `XKey.macroInEnglishMode`, `XKey.autoCapsMacro`, `XKey.addSpaceAfterMacro`
- **Root cause #2:** `applySettings()` truyền 4 macro settings vào `engineSettings.*`
- **Root cause #3:** Khởi tạo `private let macroManager = MacroManager()`, gọi `VNEngine.setSharedMacroManager(macroManager)` + `loadMacrosFromSharedDefaults()` trong `init()` và `reloadSettings()`
  - `loadMacrosFromSharedDefaults()`: `macroManager.clearAll()` → `SharedSettings.shared.getMacrosData()` → JSONDecoder decode `[MacroItemData]` → `addMacro` cho từng macro enabled, log count
  - `MacroItemData`: struct Codable, `isEnabled` default `true` (decodeIfPresent)
- **Root cause #4:** Space handler (case 0x31) macro branch — check `result.isMacroReplacement` trước restore branch, dùng `result.newCharacters` (không phải `getCurrentWord()` — engine đã reset), replace qua `setMarkedText` + `commitComposition` (marked mode) hoặc `replaceTextDirect`/`insertText` (direct mode), reset `currentWordLength`/`markedTextStartLocation`, predict `lastKnownSelectionLocation`, return false

**File:** `XKey/Core/Engine/VNEngine.swift`
- `ProcessResult`: thêm `var isMacroReplacement: Bool = false`
- `convertHookStateToResult()`: set `result.isMacroReplacement = (hookState.code == UInt8(vReplaceMacro))`
  - Cách này cover cả 2 macro branches (direct hit + macro-after-restore) — set tập trung thay vì per-branch

### 3. Version.xcconfig Linking cho XKeyIM target
**File:** `XKey.xcodeproj/project.pbxproj`
- Thêm `baseConfigurationReference = VERSION0001 /* Version.xcconfig */;` vào:
  - `100D2BB52EF2B89600C26B87 /* Debug */` (XKeyIM target)
  - `100D2BB62EF2B89600C26B87 /* Release */` (XKeyIM target)

**File:** `Version.xcconfig`
- `CURRENT_PROJECT_VERSION = 20260813` (đã re-apply, đang giữ nguyên)

## Build & Deploy

- Build: `xcodebuild -project XKey.xcodeproj -scheme XKeyIM -configuration Debug build CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO` → **BUILD SUCCEEDED**
- Deploy (bắt buộc rm -rf trước khi copy):
  ```
  killall XKeyIM; sleep 1; rm -rf ~/Library/"Input Methods"/XKeyIM.app
  cp -R <DerivedData>/Build/Products/Debug/XKeyIM.app ~/Library/"Input Methods"/XKeyIM.app
  ```
- Verify: `plutil -p` → `CFBundleShortVersionString => 1.2.24`, `CFBundleVersion => 20260813`
- Re-register LaunchServices + select input source `com.codetay.inputmethod.XKey` (Carbon TIS API)
- XKeyIM process running (PID 3177), đang là input source được select

## Runtime data (App Group plist)

`~/Library/Group Containers/7E6Z9B4F2H.com.codetay.inputmethod.XKey/Library/Preferences/*.plist`:
- `XKey.macroEnabled => true`
- `XKey.macroInEnglishMode => true`
- `XKey.addSpaceAfterMacro => true`
- `XKey.macrosData` có data (6866 bytes JSON array) — macros sẵn sàng load

## Lesson learned

- **COMMIT ngay sau khi build ổn định** — bản 20260813 đã bị mất vì không commit. Nên có git tag cho mỗi bản build stable.
- `git reset --hard` là destructive — luôn stash/commit trước khi reset.
- Space handler macro branch **đã tồn tại sẵn** trong base 20260721 — chỉ cần `isMacroReplacement` flag để kích hoạt.
