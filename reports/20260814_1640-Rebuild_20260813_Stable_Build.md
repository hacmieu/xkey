# Report — Rebuild 20260813 Stable Build (XKeyIM)

**Date:** 2026-08-14 16:40
**Git state:** detached HEAD tại `768d4d0` (tag `v1.2.24-20260721`)

## Tóm tắt

Bản build 20260813 (stable, có Macro + cursor fix) bị mất vì `git reset --hard HEAD` — chưa bao giờ được commit. Toàn bộ thay đổi đã được **re-implement từ documentation** (memory/reports/plans 20260813) và build thành công.

## Kết quả build & deploy

| Kiểm tra | Kết quả |
|----------|---------|
| Build | ✅ BUILD SUCCEEDED (Debug, no signing) |
| CFBundleShortVersionString | ✅ `1.2.24` |
| CFBundleVersion | ✅ `20260813` |
| Deploy | ✅ `~/Library/Input Methods/XKeyIM.app` (rm -rf trước copy) |
| Input source | ✅ Selected `com.codetay.inputmethod.XKey` (Carbon TIS) |
| Process | ✅ XKeyIM running (PID 3177) |
| App Group macro settings | ✅ `macroEnabled=true`, `macrosData` có 6866 bytes data |

## Chi tiết code đã re-implement

### XKeyIMController.swift
1. **skipNextCursorCheck** (fix VS Code cursor false-positive):
   - Property `private var skipNextCursorCheck: Bool = false`
   - `handle()`: consume flag + `!skipThisCursorCheck` trong điều kiện cursor detection
   - Set `true` trong `setMarkedText()` và `commitComposition()`
2. **Macro system** (4 root causes từ report 20260813_1648):
   - `XKeyIMSettings`: 4 property mới + đọc từ App Group plist keys
   - `applySettings()`: truyền macro settings vào engine
   - `MacroManager` init + `setSharedMacroManager` + `loadMacrosFromSharedDefaults()` (App Group plist → JSON → addMacro)
   - Space handler: branch `isMacroReplacement` riêng trước spell-restore branch

### VNEngine.swift
- `ProcessResult.isMacroReplacement: Bool = false`
- `convertHookStateToResult()`: set flag khi `hookState.code == vReplaceMacro`

### project.pbxproj
- `baseConfigurationReference = VERSION0001` thêm vào XKeyIM target Debug (`100D2BB52EF2B89600C26B87`) + Release (`100D2BB62EF2B89600C26B87`) — fix 20260813_2226

### Version.xcconfig
- `CURRENT_PROJECT_VERSION = 20260813`

## Key insight

- Space handler macro branch **đã tồn tại sẵn** trong base 20260721 — chỉ thiếu `isMacroReplacement` flag để kích hoạt.
- `isMacroReplacement` set tập trung tại `convertHookStateToResult()` (1 chỗ) thay vì per-branch — đúng cả 2 trường hợp: macro trực tiếp và macro-sau-restore.

## Khuyến nghị

1. **Commit ngay** sau khi xác nhận build ổn định — tạo tag `v1.2.24-20260813`.
2. `git stash` thay vì `git reset --hard` khi muốn bỏ thay đổi — tránh mất work không recover được.
3. Thêm pre-commit hook hoặc checklist verify: sau mỗi lần build stable → commit + tag.
